import {React,  useState, useEffect } from 'react';
import { Text, View,SafeAreaView, Dimensions, Image,Button 
  ,ScrollView,StyleSheet,TouchableOpacity} from 'react-native';
const Items = ({ route ,navigation})=>{
    return(
      <View>

      </View>
    )

   //<Text style={{ fontSize: 30 }}>{item.name}</Text>
   // <Text style={styles.intro}>{item.intro}</Text>
   // <Text style={{ fontSize: 30 }}>Properties</Text>
   // <Text>{item.properties[0].name + ': ' + item.properties[0].value}</Text>
   // <Text>{item.properties[1].name + ': ' + item.properties[1].value}</Text>
   // <Text>{item.properties[2].name + ': ' + item.properties[2].value}</Text>
}
export default Items;